interface Destination {
  _id: string;
  name: string;
  description: string;
  location: string;
  country: string;
  hImageUrl: string;
  vImageUrl: string;
  isVisited: boolean;
}

export default Destination;
